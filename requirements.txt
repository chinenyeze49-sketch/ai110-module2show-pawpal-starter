# Core
streamlit>=1.30
tabulate>=0.9

# Tests
pytest>=7.0

# Optional AI providers — only needed if you switch off the default mock.
# pip install -r requirements.txt installs these too; comment out anything
# you don't want.
anthropic>=0.34
openai>=1.40
